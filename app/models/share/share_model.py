

class ShareModel:
    def __init__(self, root_id, storage_host, storage_root, user_name, user_passwd):
        self.root_id = root_id
        self.storage_host = storage_host
        self.storage_root = storage_root
        self.user = user_name
        self.passwd = user_passwd
